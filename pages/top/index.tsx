import {NextPage, NextPageContext} from "next";
import React from "react";

type Props = {
    name: string
};

const FirstPage: NextPage<Props> = (props: Props) => {

    return(
        <div>
            aaa
        </div>
    )
};


export default FirstPage;
